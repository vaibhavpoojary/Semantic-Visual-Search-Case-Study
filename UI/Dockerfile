FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy all UI source code
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server (accessible from host)
CMD ["npm", "run", "dev", "--", "--host"]
